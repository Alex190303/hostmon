---
//@ts-nocheck
import Layout from "../layouts/Layout.astro";
import Artist from "../components/ArtistCard.astro";

const url = `${import.meta.env.SUPABASE_URL}/rest/v1/artist_tabel?select=*`;
const apiKey = import.meta.env.SUPABASE_PUBLISHABLE_KEY;

const options = {
  headers: {
    apikey: apiKey,
  },
};

const data = await fetch(url, options).then((res) => res.json());

console.log(data);
---

<html lang="en">
  <header> 
  </header>

  <body>

<Layout title="Lineup">

  <h1>Line Up</h1>

  <div class="bgcolorlineup">

     <section class="filtrering">

      <button>Fredag</button>
      <button>Lørdag</button>
      <button>Alle Dage</button>

    </section>


<div class="artistgrid">

  {data.map((artist) => <Artist artist={artist} />)}

</div>
</div>

<a href="#"><img class="korn" src="/public/iconer_illustration/kornblomst.svg" alt="korn"></a>

</Layout>

  </body>
</html>


<style>
  .filtrering {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
  width: 360px;
  padding-right: 1rem;
    padding-left: 120px;
    padding-bottom: 2rem;
     font-family: "Roboto", sans-serif;
  font-optical-sizing: auto;
  font-weight: <weight>;
  font-style: normal;
  font-variation-settings: "wdth" 100;
  color: white;
  Background-color: #787878;
  margin-bottom: 3rem;
  height: 60px; 
  }

button {
  padding-top: 1.3rem;
  background-color: transparent;
  border: none;
  font-size: 1rem;
  color: white;
}

  .bgcolorlineup {
Background-color: #d0d0d0;
padding-bottom: 6rem;
margin-bottom: 3rem;
  }


  /*@media (max-width: 700px) {*/
  .artistgrid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    justify-self: center;
      gap: 5rem;
  margin: 0 auto;
  }

  /*}*/

  button:hover {
    color: #E5FF00;
  }

  .korn {
    width: 650px;
    justify-self: center;
    padding-bottom: 5rem;
  }


</style>